<UserControl x:Class="PassVaultWindows.Views.PhotoVaultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{DynamicResource WindowBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource SurfaceBrush}" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="&#8592;" Style="{StaticResource IconButtonStyle}" Click="Back_Click" />
                <TextBlock Grid.Column="1" Text="Photo vault" Style="{StaticResource HeadlineTextStyle}" FontSize="16" VerticalAlignment="Center" Margin="8,0" />
                <Button Grid.Column="2" Content="&#128247;" Style="{StaticResource IconButtonStyle}" Click="AddPhoto_Click" ToolTip="Add photo" />
            </Grid>
        </Border>

        <ProgressBar x:Name="LoadingBar" Grid.Row="1" IsIndeterminate="True" Height="3" Visibility="Collapsed" />

        <Grid Grid.Row="2">
            <TextBlock x:Name="EmptyText" Style="{StaticResource SecondaryTextStyle}" FontSize="14" TextWrapping="Wrap" TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="32"
                       Text="No photos yet. Tap the camera icon to add one." Visibility="Collapsed" />
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="PhotosGrid" Margin="8">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="110" Height="110" Margin="4" Background="{DynamicResource SurfaceVariantBrush}" CornerRadius="6" ClipToBounds="True"
                                    MouseLeftButtonUp="Thumbnail_MouseLeftButtonUp" Tag="{Binding}" Cursor="Hand">
                                <Image Source="{Binding Thumbnail}" Stretch="UniformToFill" />
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
